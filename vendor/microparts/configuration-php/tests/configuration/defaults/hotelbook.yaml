defaults: # Stage "defaults" - место хранения значений по-умолчанию
  hotelbook_params:
    area_mapping:
      KRK: Krakow
      MSK: Moscow
      CHB: Челябинск
  logging: debug

shared:
  agents_id:
    - hotelbook: 1
    - zobroniruy: 2
